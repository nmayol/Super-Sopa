#ifndef TRIE
#define TRIE

#include <string>
using namespace std;

class TrieDictionary {
    public:
        TrieDictionary ();

        void afegir (string s);

        bool comprovar(string s);
    private:
        
};

#endif