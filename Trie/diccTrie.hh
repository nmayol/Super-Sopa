#ifndef TRIE
#define TRIE

#include <string>

class TrieDictionary {
    public:
        TrieDictionary ();

        void afegir (string s);

        bool comprovar(string s);
    private:
        
};

#endif