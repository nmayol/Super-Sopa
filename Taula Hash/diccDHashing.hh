#ifndef HASH_TABLE
#define HASH_TABLE

#include <string>

class HashTableDictionary {
    public:
        HashTableDictionary ();

        void afegir (string s);

        bool comprovar(string s);
    private:
        
};

#endif